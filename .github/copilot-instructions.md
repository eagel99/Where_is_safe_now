# Copilot Instructions

## Project Guidelines
- For the Pikud HaOref project, the Oref API rate-limits or blocks parallel requests. Hebrew letter queries must be done one at a time sequentially, not batched in parallel.